#include <iostream>
#include <cmath>
using namespace std;

int main() { //ceilfunction take the double input and return the smallest integer value grater than or equal to the given number;
  double num = 0.25;
  double result = ceil(num);
  cout << "The Ceil of " << num << " = " << result << endl;
  return 0;
}
